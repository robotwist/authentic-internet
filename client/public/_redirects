# Netlify redirects file

# Proxy API requests (fallback in case netlify.toml doesn't work)
/api/*  https://authentic-internet-api.onrender.com/api/:splat  200

# Handle client-side routing
/*    /index.html   200
